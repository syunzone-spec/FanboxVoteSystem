# 仕様パック一式（CLAUDE.md 4.2 が非公開の重要物として名指し。md 以外に投票ログ CSV・分析スクリプトを含むためフォルダごと除外。旧実装は *.md のみで .csv/.py が素通りしていた）
00_Spec/

*.md

# GAS バックエンド（防御設計の本体。GitHub Pages 配信には不要なため公開リポジトリから分離）
11_backend/

# .txt 一般（backend.txt のソース控え・投票ログ等の機微データを誤公開しないため）
*.txt

# A-1 Cloudflare Worker（IP出自判定ロジック・ASNリスト。防御設計のため公開リポジトリから分離。vault 版管理）
12_worker/

# 分析用の取得データ（Fanbox / X から取得した実データ。支援者情報を含むため公開リポジトリに載せない）
20_log/
